@model AdaptiveLearningApplication.Models.QuestionOption

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>QuestionOption</legend>

        @Html.HiddenFor(model => model.QuestionOptionID)

        <div class="editor-label">
            @Html.LabelFor(model => model.QuestionOptionStatement)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.QuestionOptionStatement)
            @Html.ValidationMessageFor(model => model.QuestionOptionStatement)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.QuestionID, "Question")
        </div>
        <div class="editor-field">
            @Html.DropDownList("QuestionID", String.Empty)
            @Html.ValidationMessageFor(model => model.QuestionID)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
