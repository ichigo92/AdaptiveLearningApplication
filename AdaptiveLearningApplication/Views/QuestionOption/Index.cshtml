@model IEnumerable<AdaptiveLearningApplication.Models.QuestionOption>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuestionOptionStatement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Question.Question)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionOptionStatement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Question.Question)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QuestionOptionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.QuestionOptionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QuestionOptionID })
        </td>
    </tr>
}

</table>
